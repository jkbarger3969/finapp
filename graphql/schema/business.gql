type Vendor {
  approved: Boolean!
  vendorId: ID
}

type Business {
  id: ID!
  name: String!
  budgets: [Budget!]!
  departments: [Department!]!
  vendor: Vendor
}

input BusinessAddFields {
  name: String!
}

input BusinessesWhereInput {
  eq: ID
  ne: ID
  in: [ID!]
  nin: [ID!]
  name: WhereRegexInput
  and: [BusinessesWhereInput!]
  or: [BusinessesWhereInput!]
  nor: [BusinessesWhereInput!]
}

extend type Query {
  businesses(where: BusinessesWhereInput): [Business!]!
  business(id: ID!): Business!
}

extend type Mutation {
  addBusiness(fields: BusinessAddFields!): Business!
}
